<selector xmlns:android="http://schemas.android.com/apk/res/android">
<!-- Pressed -->
    <item android:drawable="@color/grid_selected" android:state_pressed="true"/>

    <!-- selected -->
    <item android:drawable="@color/grid_selected" android:state_selected="true"/>

    <!-- focused -->
    <item android:drawable="@color/grid_selected" android:state_focused="true"/>

    <!-- default -->
    <item android:drawable="@color/white"/>
</selector>