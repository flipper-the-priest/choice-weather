<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- Pressed -->
    <item android:drawable="@drawable/background_card_selected" android:state_pressed="true"/>

    <!-- selected -->
    <item android:drawable="@drawable/background_card" android:state_selected="true"/>

    <!-- focused -->
    <item android:drawable="@drawable/background_card_selected" android:state_focused="true"/>

    <!-- default -->
    <item android:drawable="@drawable/background_card"/>
</selector>
